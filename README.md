# SoonGo Data

## Config
Use connectors/connector_config.yaml to add different connectors to organizations
and pass connector data collection arguments, if necessary.
Input table generation logic is in input_tables, which also handles their generation.
